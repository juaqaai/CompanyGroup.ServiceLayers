select * from InternetUser.Invoice
where InvoiceCredit > 0
order by InvoiceDate 


select count(*) from InternetUser.Invoice

select count(*) from InternetUser.Invoice
where InvoiceCredit > 0

1. CustTransOpen -re bekapcsolni a CT-t,
2. CustTransOpenCT lekérdezés
3. Betöltõt elkészíteni, idõzíteni
4. Lekérdezésbe beletenni a CT-s lekérdezést